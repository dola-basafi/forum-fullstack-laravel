
## how to install and run the application on a local development environment.

- git clone from repo
- cd to directory
- run the command 
- composer install
- npm install
- php artisan serve
- npm run dev

